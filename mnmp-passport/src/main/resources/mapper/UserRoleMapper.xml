<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tarena.mnmp.passport.dao.UserRoleMapper">

    <insert id="save">
        insert into notice_user_role (role_id,user_id,create_time,update_time)
        values (#{roleId},#{userId},now(),now())
    </insert>

</mapper>

